import { makeMetadata, RoutePlaceholder } from "../../../lib/page-shell.js";

export const generateMetadata = makeMetadata("legalTerms");

export default function LegalTermsPage() {
  return <RoutePlaceholder routeId="legalTerms" />;
}
