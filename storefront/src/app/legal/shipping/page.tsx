import { makeMetadata, RoutePlaceholder } from "../../../lib/page-shell.js";

export const generateMetadata = makeMetadata("legalShipping");

export default function LegalShippingPage() {
  return <RoutePlaceholder routeId="legalShipping" />;
}
