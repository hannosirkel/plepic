import { makeMetadata, RoutePlaceholder } from "../../../lib/page-shell.js";

export const generateMetadata = makeMetadata("legalPrivacy");

export default function LegalPrivacyPage() {
  return <RoutePlaceholder routeId="legalPrivacy" />;
}
