import { makeMetadata, RoutePlaceholder } from "../../../lib/page-shell.js";

export const generateMetadata = makeMetadata("legalReturns");

export default function LegalReturnsPage() {
  return <RoutePlaceholder routeId="legalReturns" />;
}
