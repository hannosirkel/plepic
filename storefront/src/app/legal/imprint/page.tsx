import { makeMetadata, RoutePlaceholder } from "../../../lib/page-shell.js";

export const generateMetadata = makeMetadata("legalImprint");

export default function LegalImprintPage() {
  return <RoutePlaceholder routeId="legalImprint" />;
}
